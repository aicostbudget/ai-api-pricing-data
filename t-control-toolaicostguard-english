warning: in the working copy of 'scripts/generate_website_projection_v2.py', CRLF will be replaced by LF the next time Git touches it
[1mdiff --git a/scripts/generate_website_projection_v2.py b/scripts/generate_website_projection_v2.py[m
[1mindex 1f3678a..b867216 100644[m
[1m--- a/scripts/generate_website_projection_v2.py[m
[1m+++ b/scripts/generate_website_projection_v2.py[m
[36m@@ -323,6 +323,14 @@[m [mdef price_source_refs(identity: dict[str, Any], price: dict[str, Any] | None) ->[m
     return sorted(set(refs))[m
 [m
 [m
[32m+[m[32mdef format_context_window(tokens: int) -> str:[m
[32m+[m[32m    if tokens % 1_000_000 == 0:[m
[32m+[m[32m        return f"{tokens // 1_000_000}M"[m
[32m+[m[32m    if tokens % 1_000 == 0:[m
[32m+[m[32m        return f"{tokens // 1_000}K"[m
[32m+[m[32m    return f"{tokens:,}"[m
[32m+[m
[32m+[m
 def website_id_for(identity: dict[str, Any]) -> str:[m
     website_ids = identity.get("websiteIds") or [][m
     return website_ids[0] if website_ids else identity["canonicalOfficialId"][m
[36m@@ -849,6 +857,7 @@[m [mdef projection_row([m
             [source_timestamp(sources_by_id[ref], "checkedAt") for ref in refs if ref in sources_by_id][m
         )[m
         checked_source_refs = source_refs_at_timestamp(refs, sources_by_id, "checkedAt", checked_at)[m
[32m+[m[32m    context_window_tokens = (model or {}).get("contextWindowTokens")[m
     row = {[m
         "id": website_id_for(identity),[m
         "provider": identity["providerId"],[m
[36m@@ -876,8 +885,8 @@[m [mdef projection_row([m
             public_official_url=public_official_url,[m
             website_official_url=website_official_url,[m
         ),[m
[31m-        "contextWindow": None,[m
[31m-        "contextWindowStatus": "unknown_not_guessed",[m
[32m+[m[32m        "contextWindow": format_context_window(context_window_tokens) if context_window_tokens is not None else None,[m
[32m+[m[32m        "contextWindowStatus": "canonical_verified" if context_window_tokens is not None else "unknown_not_guessed",[m
         "canonicalInternalId": identity["internalId"],[m
         "identityType": identity["identityType"],[m
         "lifecycleStatus": identity["lifecycleStatus"],[m
[36m@@ -899,6 +908,8 @@[m [mdef projection_row([m
         "sourceRefs": refs,[m
         "sourceUrls": urls,[m
     }[m
[32m+[m[32m    if context_window_tokens is not None:[m
[32m+[m[32m        row["contextWindowTokens"] = context_window_tokens[m
     if identity.get("scheduledTransition") is not None:[m
         row["scheduledTransition"] = identity["scheduledTransition"][m
     if identity["internalId"] == "xai/grok-3":[m
[36m@@ -1298,7 +1309,7 @@[m [mdef build_phase45_audits([m
                 "canonicalVerificationStatus": "verified" if canonical_value is not None else "not_present",[m
                 "officialSourceAvailable": bool(row["sourceRefs"]),[m
                 "projectedContextWindow": row["contextWindow"],[m
[31m-                "reason": "Canonical V2 has no verified context-window field for this identity; projection leaves null rather than copying Website editorial text.",[m
[32m+[m[32m                "reason": ("Projected from the opt-in verified canonical V2 context-window field." if canonical_value is not None else "Canonical V2 has no verified context-window field for this identity; projection leaves null rather than copying Website editorial text."),[m
             }[m
         )[m
     context_audit = {[m
[36m@@ -1306,6 +1317,7 @@[m [mdef build_phase45_audits([m
         "contextWindowRows": len(context_rows),[m
         "verifiedCanonicalContextWindowCount": sum(1 for row in context_rows if row["canonicalContextWindow"] is not None),[m
         "projectedNullCount": sum(1 for row in context_rows if row["projectedContextWindow"] is None),[m
[32m+[m[32m        "projectedCanonicalMatchCount": sum(1 for row in context_rows if (row["canonicalContextWindow"] is None and row["projectedContextWindow"] is None) or (row["canonicalContextWindow"] is not None and row["projectedContextWindow"] == format_context_window(row["canonicalContextWindow"]))),[m
         "rows": context_rows,[m
     }[m
 [m
[36m@@ -1358,7 +1370,7 @@[m [mdef build_phase45_audits([m
             safe_stats["unexplained"] == 0[m
             and len(row_reconciliation_rows) == len(projection_rows)[m
             and len(unsafe_audit["blockerUnsafeDifferences"]) == 0[m
[31m-            and context_audit["projectedNullCount"] == context_audit["contextWindowRows"][m
[32m+[m[32m            and context_audit["projectedCanonicalMatchCount"] == context_audit["contextWindowRows"][m
             and all(all(value is True for key, value in item.items() if key not in {"projectionRowId", "canonicalInternalId", "selectedPriceRecordId"}) for item in default_safe_review)[m
             and all(item["phase4BPolicy"] != "shadow_only" or item["defaultSafe"] for item in unsafe_null_policy)[m
         ),[m
[36m@@ -1489,8 +1501,16 @@[m [mdef validate_projection(artifact: dict[str, Any], report: dict[str, Any]) -> Non[m
         missing = sorted(field for field in required if field not in row)[m
         if missing:[m
             raise ValueError(f"projection row {row.get('id')} missing required fields {missing}")[m
[31m-        if row["contextWindow"] is not None:[m
[31m-            raise ValueError(f"contextWindow must not be guessed: {row['id']}")[m
[32m+[m[32m        context_window_tokens = row.get("contextWindowTokens")[m
[32m+[m[32m        if row["contextWindow"] is None and context_window_tokens is not None:[m
[32m+[m[32m            raise ValueError(f"contextWindowTokens must not exist without contextWindow: {row['id']}")[m
[32m+[m[32m        if row["contextWindow"] is not None and ([m
[32m+[m[32m            not isinstance(context_window_tokens, int)[m
[32m+[m[32m            or context_window_tokens <= 0[m
[32m+[m[32m            or row["contextWindow"] != format_context_window(context_window_tokens)[m
[32m+[m[32m            or row["contextWindowStatus"] != "canonical_verified"[m
[32m+[m[32m        ):[m
[32m+[m[32m            raise ValueError(f"contextWindow must match verified canonical tokens: {row['id']}")[m
         if row["verificationStatus"] in {"review_required", "unconfirmed_price"} and row["verifiedAt"] is not None:[m
             raise ValueError(f"review/unconfirmed row must not have verifiedAt: {row['id']}")[m
         for timestamp_field, source_refs_field in ([m
